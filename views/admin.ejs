
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard</title>
    <link rel="stylesheet" href="/css/admin.css">
    <style>
        /* General Styling */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Arial', sans-serif;
        }

        body {
            display: flex;
            flex-direction: column;
            min-height: 100vh;
            background-color: #f4f7fc;
        }

        /* Header */
        header {
            width: 100%;
            height: 70px;
            background-color: #0078d4;
            color: white;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 0 30px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        header h1 {
            font-size: 1.8rem;
        }

        nav ul {
            list-style: none;
            display: flex;
            gap: 30px;
        }

        nav ul li a {
            text-decoration: none;
            color: white;
            font-size: 1rem;
            transition: color 0.3s ease;
        }

        nav ul li a:hover {
            color: #f9f9f9;
        }

        /* Sidebar */
        .sidebar {
            width: 250px;
            background-color: #005bb5;
            color: white;
            padding: 20px;
            position: fixed;
            top: 70px; /* Make sure the sidebar stays below the header */
            bottom: 0;
            box-shadow: 2px 0 6px rgba(0, 0, 0, 0.1);
            overflow-y: auto; /* Allow scrolling for sidebar if necessary */
        }

        .sidebar ul {
            list-style: none;
            padding: 0;
        }

        .sidebar ul li {
            padding: 15px 20px;
            transition: background-color 0.3s ease;
        }

        .sidebar ul li a {
            text-decoration: none;
            color: white;
            font-size: 1rem;
            display: block;
        }

        .sidebar ul li:hover {
            background-color: #0078d4;
        }

        /* Main Content */
        .main-content {
            margin-left: 250px;
            padding: 20px 30px;
            flex: 1;
            background-color: #f4f7fc;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: flex-start;
            gap: 20px;
            position: relative; /* So the content can scroll independently of the sidebar */
            height: 100vh;
            overflow-y: auto;
        }

        .main-content h2 {
            font-size: 2rem;
            margin-bottom: 10px;
        }

        .main-content p {
            color: #555;
            font-size: 1.1rem;
            line-height: 1.6;
            text-align: center;
            margin-bottom: 20px;
        }

        /* Image Styling */
        .dashboard-image {
            max-width: 100%;
            height: auto;
            border-radius: 15px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease;
        }

        .dashboard-image:hover {
            transform: scale(1.05);
        }

        /* Footer */
        footer {
            text-align: center;
            padding: 20px;
            background-color: #0078d4;
            color: white;
            width: 100%;
            margin-top: auto;
        }

        footer p {
            margin: 0;
            font-size: 1rem;
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .container {
                flex-direction: column;
            }

            .sidebar {
                width: 100%;
                position: relative;
                top: 0;
                margin-top: 20px;
                box-shadow: none;
            }

            .main-content {
                margin-left: 0;
                padding: 20px;
            }

            .main-content h2 {
                font-size: 1.5rem;
            }

            .sidebar ul li {
                padding: 12px 20px;
            }

            footer p {
                font-size: 0.9rem;
            }
        }

        @media (max-width: 480px) {
            header h1 {
                font-size: 1.6rem;
            }

            nav ul {
                flex-direction: column;
                gap: 15px;
            }

            nav ul li a {
                font-size: 0.9rem;
            }
        }
    </style>
</head>
<body>
    <!-- Header -->
    <header>
        <h1>Admin Dashboard</h1>
        
        
    </header>

    <!-- Sidebar and Main Content -->
    <div class="container">
        <!-- Sidebar -->
        <aside class="sidebar">
            <ul>
                <li><a href="/">Home</a></li>
                <li><a href="/users">Manage Users</a></li>
                <li><a href="/users/blocked">Blocked Users</a></li>
                <li><a href="/query/queries">Query</a></li>
              
                
                <li><a href="/user/logout">Logout</a></li>
            </ul>
        </aside>

        <!-- Main Content -->
        <main class="main-content">
            <h2>Welcome, Admin!</h2>
            <p>This is your dashboard where you can manage users, settings, and more.</p>
            <!-- Image -->
            <img src="https://res.cloudinary.com/dhhe18dia/image/upload/v1750833423/admin_photo_j8fyg8.jpg" alt="Welcome Image" class="dashboard-image">
        </main>
    </div>

    <!-- Footer -->
    <footer>
        <p>&copy; 2024 CampusLink. All rights reserved.</p>
    </footer>
</body>
</html>
